import Header from "../Header";
import { render, cleanup } from "react-testing-library";

afterEach(cleanup);

describe("Tab Change", () => {
  it("should", () => {
    expect(true).toBeTruthy();
  });
});
